/* Processo CPU-bound com execução de 5 segundos (Precisa ser compilado antes de executar o Escalonador) */
int main(){
    //Caso necessário, mude o valor da comparação do for para ajustar o tempo de execução
    for(long i = 0; i < 3000000000; i++);
    return 0;
}